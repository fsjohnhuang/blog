* 学习路线
  - React
    1) basic knowledge
    2) css in js
       1) Styled Components
       2) Radium
       3) Emotion
       4) JSS
       5) Aphrodite
    3) react-router(done)
    4) redux
    5) axios
    6) i18n
    7) tool chain:
       1) webpack
       2) babel
       3) npm
       4) npx
       5) nvm(done)
       6) devtools
       7) eslint
       8) prettier
    8) animation
    9) ssr
    10) immutable
    11) vdom+diff+patch
    12) app with antd
    13) customize component
        1) table(dynamic columns, fixed header, freeze columns, on-fly edit)
        2) tree(drag and drop)
        3) numeric input
        4) form https://jaredpalmer.com/formik/
           1) yup
        5) datetime picker
        6) layout
        7) dialog
  - security
  - http, https
  - memory leak debug
  - pwa
  - webapp
  - hybird app

* TODO 手脚架
1) Create React App
   - github: https://github.com/facebook/create-react-app
   - 入门级的脚手架工具，适用于学习和简单的应用开发
   - 使用方式
     #+begin_src bash
     # create a new project
     npx create-react-app my-app

     # update build tooling
     npm install react-script@lastest
     #+end_src
2) Codesnadbox
   - https://codesandbox.io
   - 在线的开发平台
3) Rekit
   - 基于Create React App添加Redux,React Router,Less/Scss,Feature Oriented Architecture,Dedicated IDE的功能
   - 适用于大型项目

* TODO Webpack打包和部署
打包注意事项：
1) 设置Nodejs环境为production
2) 禁用开发专用代码，如logger
3) 设置应用根路径
   package.json的home路径下，要和部署后的访问路径一致。

* JSX
  1. JSX本身为表达式
  2. JSX将在编译期编译为 ~React.createElement~ 等方法
     #+begin_example js
     import React from "react"

     const element = (
       <h1 className="greeting">
         Hello!
       </h1>
     )

     /* 编译后得到
      * const element = React.createElement(
      *                 "h1",
      *                 {className: "greeting"},
      *                 "Hello!")
      */

     /* React.createElement 返回形如以下格式的 React Element
      * { type: "h1",
      *   props: {
      *     className: "greeting",
      *     children: "Hello!"
      *   }
      * }
      */
     #+end_example

  #+begin_src js
  name = "john"

  // 单行JSX不用括号包裹
  el1 = <h1>Hello, {name + " huang"}</h1>

  // 多行JSX需要括号包裹
  el2 = (
    <h1>
      Hello, {name + " huang"}
    </h1>
  )

  // 两种设置属性的方式
  // 静态属性：双引号包裹值
  // 动态属性：花括号包裹JavaScript表达式
  // 注意：JSX中的属性命名采用camelCase，如class变为className，tabindex变为tabIndex
  el3 = <div tabIndex="0" name={name + " huang"}></div>

  // 若没有子元素，可以直接以/>结尾
  el4 = <img src={user.avatarUrl} />

  // 可以包含子元素
  el5 = (
    <div>
       <h1>Hello!</h1>
    </div>
  )
  #+end_src

* Component

** Function Component
   #+begin_example js
   /* @param props
    * @return React Element
    */
   function Welcome(props){
     return <h1>Hello, {props.name}</h1>
   }

   const el = <Welcome name="john" />
   ReactDOM.render(
     el,
     document.getElementById("root")
   )
   #+end_example

** Class Component
   #+begin_example js
   class Welcome extends React.Component {
     constructor(props){
       super(props)
       this.state = {date: new Date()}
     }

     // 生命周期函数，挂载到页面时触发
     componentDidMount(){
       this.timerID = setInterval(
         () => this.setState({date: new Date()})
         1000
       )
     }

     // 生命周期函数，即将从页面卸载时触发
     componentWillUnmount(){
       clearInterval(this.timerID)
     }

     render(){
       return (
         <div>
           <h1>Hello, {this.props.name}</h1>
           <div>datetime: {this.state.date.toLocalTimeString()}</div>
         </div>
       )
     }
   }

   const el = <Welcome name="john" />
   ReactDOM.render(
     el,
     document.getElementById("root")
   )
   #+end_example

* Props and States
  1) Props 必须为只读状态，不能被修改。
  2) States 存储组件内部状态，只能通过 ~setState~ 异步更新
     1) 不依赖现在的值来更新state
        #+begin_example js
        this.setState({name: "abc"})
        #+end_example
     2) 依赖现在的值来更新state
        #+begin_example js
        this.setState((state, props) => {
          return {name: state.name + props.name}
        })
        #+end_example
     3) 分别更新部分state，React会合并更新操作
        #+begin_example js
        constructor(props){
          super(props)
          this.state = {
            posts: [],
            comments: []
          }
        }

        componentDidMount(){
          this.setState({posts: [1]})
          this.setState({comments: [2]})
        }
        #+end_example
* Event Handler

** Function Component的方式
   #+begin_example js
   function ActionLink(props){
     /* @param e {SyntheticEvent} - React根据W3C spec规定编写Event类，已兼容所有浏览器。
      * @return null - 返回false不会导致浏览器默认事件处理函数不执行，需要显式调用preventDefault才行。
      */
     const handleClick = e => {
       e.preventDefault()
       console.log("Click")
     }

     return <a href="#" onClick={handleClick}>Click Me</a>
   }
   #+end_example

** Class Component的方式
   #+begin_example js
   // 方式1
   class ActionLink1 extends React.Component{
     constructor(props){
       super(props)
       this.handleClick = this.handleClick.bind(this, "preset argument")
       this.state = {name: "john"}
     }

     handleClick(preset, e){
       e.preventDefault()
       console.log("Click by " + this.state.name)
     }

     render(){
       return <a href="#" onClick={this.handleClick}>Click Me</a>
     }
   }

   // 方式2
   class ActionLink2 extends React.Component{
     constructor(props){
       super(props)
       this.state = {name: "john"}
     }

     // 新语法：public class fields，会自动绑定this为当前实例
     handleClick = (e) => {
       e.preventDefault()
       console.log("Click by " + this.state.name)
     }

     render(){
       return <a href="#" onClick={this.handleClick}>Click Me</a>
     }
   }

   // 方式3
   class ActionLink1 extends React.Component{
     constructor(props){
       super(props)
       this.state = {name: "john"}
     }

     handleClick(e, other_arg){
       e.preventDefault()
       console.log("Click by " + this.state.name)
     }

     render(){
       return <a href="#" onClick={(e) => this.handleClick(e, "other argument")}>Click Me</a>
     }
   }
   #+end_example
** 注意
   1) 原生的onchange事件是控件失焦后，发现控件内容有变化才触发；React中的onChange事件，则控件内容发生变化即触发。

* 根据条件不渲染组件
  只需返回null即可。

** Function Component
   #+begin_example js
   function Welcome(props){
     if (props.warn){
       return null
     }

     return <h1>Hello</h1>
   }
   #+end_example

** Class Component
   #+begin_example js
   class Welcome extends React.Component{
     render(){
       if (this.props.warn){
         return null
       }

       return <h1>Hello</h1>
     }
   }
   #+end_example
* 列表中的key属性
  对于根据数组生成的元素列表，需要通过设置该数组唯一的key来让React识别元素的修改、新增和删除。
  注意：
  1) key是设置在通过循环数组生成的元素列表的元素上；
  2) key的值为数组内唯一，而不是全局唯一。

** diff算法
   1) 广度优先同层比对；
   2) 从根往子组件逐层比对；
   3) 发现一层不同则不再往下一层比对，从不同的层级开始重新生成节点渲染。

** 为什么兄弟节点需要设置key
   https://reactjs.org/docs/reconciliation.html#recursing-on-children
   当兄弟节点排列顺序发生变化时，通过key识别来重新排序从而减少节点删除和重新创建。

* Form

** Controlled Components
   元素的值由React管理，用户修改输入内容时必须修改React Element的状态然后由React控制组件的变化。
   注意：若设置为null后，元素的内容就根据用户输入变化，而不受React管控。
   #+begin_example js
   class LoginForm extends React.Component {
     constructor(props){
       super(props)
       this.state = {account: "",
                     password: ""}
       this.handleAccountChange = this.handleAccountChange.bind(this)
       this.handlePasswordChange = this.handlePasswordChange.bind(this)
     }

     handleAccountChange(e){
       this.setState({account: e.target.value})
     }

     handlePasswordChange(e){
       this.setState({password: e.target.value})
     }

     handleSubmit(e){
       e.preventDefault()
       console.log(this.state.account)
       console.log(this.state.password)
     }

     render(){
       return (
         <form onSubmit={this.handleSubmit}>
           <label>
             Account: <input type="text" value={this.state.account} onChange={this.handleAccountChange} />
           </label>
           <label>
             Password: <input type="password" value={this.state.password} onChange={this.handleSubmit} />
           </label>
           <input type="submit" value="submit" />
         </form>
       )
     }
   }
   #+end_example

*** Textarea
    通过属性value设置元素内容，不能通过innerText的方式。
    #+begin_example js
    <textarea value={this.state.value} onChange={this.handleChange}/>
    #+end_example

*** Select
    通过属性select元素的value代替option中通过selected标记选中项。
    当启用multiple时，可向value传递数组选中多个项目。
    #+begin_example js
    <select value={this.state.value} onChange={this.handleChange} multiple={true}>
      <option value="1">No1</option>
      <option value="2">No2</option>
    </select>
    #+end_example

** Uncontrolled Components
   元素的值由原生HTML控件管理，React不参与。

*** 通过ref引用控件来获取值
    #+begin_example js
    class NameForm extends React.Component{
      constructor(props){
        super(props)
        this.handleSubmit = this.handleSubmit.bind(this)
        this.input = React.createRef()
      }

      handleSubmit(e){
        e.preventDefault()
        console.log(this.input.current.value)
      }

      render(){
        return (
          <form onSubmit={this.handleSubmit}>
            <input type="text" ref={this.input} />
            <input type="submit" value="submit" />
          </form>
        )
      }
    }
    #+end_example

*** 由React设置默认值，但不控制后续控件内容的变化
    1) ~input[type="checkbox"], input[type="radio"]~ 通过 ~defaultChecked~ 设置
    2) ~input[type="text"], textarea, select~ 通过 ~defaultValue~ 设置

    #+begin_example js
    class NameForm extends React.Component{
      constructor(props){
        super(props)
        this.handleSubmit = this.handleSubmit.bind(this)
        this.input = React.createRef()
      }

      handleSubmit(e){
        e.preventDefault()
        console.log(this.input.current.value)
      }

      render(){
        return (
          <form onSubmit={this.handleSubmit}>
            <input type="text" ref={this.input} defaultValue={this.props.defaultValue} />
            <input type="submit" value="submit" />
          </form>
        )
      }
    }
    #+end_example

*** File
    由于 ~input[type="file"]~ 为只读控件，必须通过File API来操作，因此File控件必须为Uncontrolled Component。
    #+begin_example js
    class MyForm extends React.Component{
      constructor(props){
        super(props)
        this.handleSubmit = this.handleSubmit.bind(this)
        this.file = React.createRef()
      }

      handleSubmit(e){
        e.preventDefault()
        console.log(this.file.current.files[0].name)
      }

      render(){
        return (
          <form onSubmit={this.handleSubmit}>
            <input type="file" ref={this.file} />
            <input type="submit" value="submit" />
          </form>
        )
      }
    }
    #+end_example
** Yup
browser和Node通用的JavaScript对象框架验证工具和对象解析器，并且支持同步和异步验证。

* Fragment
  React规定JSX表达式最多只能返回一个等层React Element，就是说不能返回一组React Element列表。因此通常会外层添加一个容器。
  #+begin_example js
  const el = (
    <div>
      <h1>Hello!</h1>
      <h2>Guys</h2>
    </div>
  )
  #+end_example
  但是有些情况下多出来的容器会导致UI变形，因此可通过 ~<React.Fragment>~ 作为容器，在渲染时会被移除。
  #+begin_example js
  const el = (
    <React.Fragment>
      <h1>Hello!</h1>
      <h2>Guys</h2>
    </React.Fragment>
  )
  #+end_example
  缩写
  #+begin_example js
  const el = (
    <>
      <h1>Hello!</h1>
      <h2>Guys</h2>
    </>
  )
  #+end_example

** 根据数组内容生成Fragment列表也需要设置key属性
  #+begin_example js
  const els = [1,2,3].map(item => (
      <React.Fragment key={item}>
        <h1>Hello!{item}</h1>
        <h2>Guys</h2>
      </React.Fragment>
    )
  )
  #+end_example
* Forwarding Ref
  用于向外暴露组件的内部元素(DOM Element或React Element)，只能用在Function Component上
  #+begin_example js
  const FancyButton = React.forwardRef((props, ref) => (
    <button ref={ref}>
      {props.children}
    </button>
  ))

  const ref = React.createRef()
  const el = (
    <FancyButton ref={ref}>
      <h1>Hello!</h1>
    </FancyButton>
  )

  // ref.current 指向<button></button>元素
  #+end_example
  注意：属性 ~ref~ 不能通过 ~props~ 传递，必须通过 ~React.forwardRef~ 创建函数并作为第二个参数．

** HOC中使用Forwarding Ref
   #+begin_example js
   function logProps(Component){
     class LogProps extends React.Component{
       componentDidUpdate(prevProps){
         console.log(prevProps)
         console.log(this.props)
       }

       render(){
         const {forwardedRef, ...rest} = this.props

         return <Component forwardedRef={forwardedRef} ...rest />
       }
     }

     return React.createRef((props, ref) => (
       <LogProps {...props} forwardedRef={ref} />
     ))
   }

   const FancyButton = props => (
     <button ref={props.forwardedRef}>
       {props.text}
     </button>
   )

   const LogFancyButton = logProps(FancyButton)
   const ref = React.createRef()
   const el = <LogFancyButton ref={ref} text="Hello!"/>
   #+end_example
** 在DevTools中显示forwardRef入参函数的函数名称
   1) 只显示ForwardRef字样
      #+begin_example js
      const FancyButton = React.createRef((props, ref) => (
        <button ref={ref}>
          {props.text}
        </button>
      ))
      #+end_example
   2) 显示ForwardRef(函数名称)字样
      #+begin_example js
      const FancyButton = React.createRef(function FancyButton(props, ref) {
        return (
          <button ref={ref}>
            {props.text}
          </button>
        )
      }
      #+end_example
   3) HOC下显示ForwardRef(自定义内容)字样
      #+begin_example js
      function logProps(Component){
        //.....
        function forwardRef(props, ref){
           return <LogProps forwardedRef={ref} ...props />
        }
        const name = Component.displayName || Component.name
        forwardRef.displayName = `logProps(${name})`

        return React.forwardRef(forwardRef)
      }

      // 显示ForwardRef(logProps(FancyButton))
      const LogFancyButton = logProps(FancyButton)
      #+end_example
* Context API
  提供一种方式在组件树中传递数据，而不用自上而下逐层传递．适用于不同层级的组件共享变量值，如current authenticated user,locale,UI Theme,data cache等多组件需要的全局属性则适用于通过该方式传递。

** API说明
   #+begin_example js
   /* 创建上下文对象，订阅上下文对象的组件会读取最近一个Provider提供的值
    * @param defaultValue {Any} - 上下文中的默认值
    */
   const MyContext = React.createContext(defaultValue)

   /* 允许其子元素将订阅上下文实例内容的变化。
    * 注意：1. Provider可嵌套，元素获取最近的一个Provider提供的值
    *       2. 当Provider的value属性值发生变化时，订阅上下文变化的组件将无条件重新渲染，即使其父组件在shouldComponentUpdate中返回false。
    * @param value {Any} - 作为上下文的值
    */
   <MyContext.Provider value={/* some value */} />

   /* Class.contextType
    * 用于在Class Component中的所有生命周期函数中使用上下文值
    */
   class FancyButton extends React.Component{
     componentDidMount(){
       let value = this.context
     }
     componentDidUpdate(){
       let value = this.context
     }
     componentWillUnmount(){
       let value = this.context
     }
     render(){
       let value = this.context
     }
     // static contextType = MyContext 启用public class fields syntax时可用这个语法设置
   }
   FancyButton.contextType = MyContext // 传统设置方式

   /* 用于在Function Component中使用上下文值
    */
   <MyContext.Consumer>
     {value => /* render something based on the context value */}
   </MyContext.Consumer>
   #+end_example

** 示例——UI Theme
   #+begin_example js
   // theme-context.js
   import React from "react"

   export const theme = {
     light: {
       foreground: "#000",
       background: "#eee"
     },
     dark: {
       foreground: "#fff",
       background: "#222"
     }
   }

   export const ThemeContext = React.createContext()
   #+end_example
   #+begin_example js
   // theme-button.js
   import {ThemeContext} from "theme-context"

   export default ThemeButton = props => (
     <ThemeContext.Consumer>
       {theme => (<button {...props} style={{backgroundColor: theme.background}} />)}
     </ThemeContext.Consumer>
   )
   #+end_example
   #+begin_example js
   // app.js
   import React from "react"
   import {theme, ThemeContext} from "theme-context"
   import ThemeButton from "theme-button"

   class App extends React.Component {
     constructor(props){
       super(props)
       this.state = {theme: theme.dark}

       this.toggleTheme = this.toggleTheme.bind(this)
     }

     toggleTheme(){
       this.setState(state => {theme: state.theme == theme.dark ? theme.light : theme.dark})
     }

     render(){
       return (
         <div>
           <ThemeContext.Provider value={this.state.theme}>
             <ThemeButton onClick={this.toggleTheme}/>
             <ThemeContext.Consumer>
               {theme => <div style={{color: theme.foreground}}>Hello!</div>}
             </ThemeContext.Consumer>
           </ThemeContext.Provider>
         </div>
       )
     }
   }
   #+end_example

** 示例——UI Theme + 将修改上下文部分内容的行为作为上下文的一部分
   #+begin_example js
   // theme-context.js
   import React from "react"

   export const theme = {
     light: {
       foreground: "#000",
       background: "#eee"
     },
     dark: {
       foreground: "#fff",
       background: "#222"
     }
   }

   export const ThemeContext = React.createContext({theme: theme.light
                                                    , toggleTheme: () => {theme: theme.light})
   #+end_example
   #+begin_example js
   // theme-button.js
   import {ThemeContext} from "theme-context"

   export default ThemeButton = props => (
     <ThemeContext.Consumer>
       {ctx => (<button {...props} style={{backgroundColor: ctx.theme.background}} onClick={ctx.toggleTheme} />)}
     </ThemeContext.Consumer>
   )
   #+end_example
   #+begin_example js
   // app.js
   import React from "react"
   import {theme, ThemeContext} from "theme-context"
   import ThemeButton from "theme-button"

   class App extends React.Component {
     constructor(props){
       super(props)
       this.state = {theme: theme.dark
                     , toggleTheme: this.toggleTheme.bind(this)}
     }

     toggleTheme(){
       this.setState(state => {theme: state.theme == theme.dark ? theme.light : theme.dark})
     }

     render(){
       return (
         <div>
           <ThemeContext.Provider value={this.state}>
             <ThemeButton />
             <ThemeContext.Consumer>
               {ctx => <div style={{color: ctx.theme.foreground}}>Hello!</div>}
             </ThemeContext.Consumer>
           </ThemeContext.Provider>
         </div>
       )
     }
   }
   #+end_example

** 示例——订阅多个上下文对象
   #+begin_example js
   import React from "react"
   import ReactDOM from "react-dom"

   user = "john"
   theme = {bg: "#eee"}
   const UserContext = React.createContext(user)
   const ThemeContext = React.createContext(theme)

   const Content = props => (
     <ThemeContext.Consumer>
       {theme => (
         <UserContext.Consumer>
           {user => <div style={backgroundColor: theme.bg}>{user}</div>}
         </UserContext.Consumer>
       )}
     </ThemeContext.Consumer>
   )

   const App = props => (
     <ThemeContext.Provider>
       <UserContext.Provider>
         <Content />
       </UserContext.Provider>
     </ThemeContext.Provider>
   )

   ReactDOM.render(<App />, document.getElementById("root"))
   #+end_example

* Higher-Order Component
  一般用于对已有组件进行增强，不影响UI显示。

  若只想从顶层向最底层元素传递变量，那么HOC则优于Context API。
  1) 问题代码
     #+begin_example js
     // Page和Link组件之间的PageLyout,NavigationBar都需要接收user和avatarSize属性，十分冗余。
     const Page = props => <PageLayout ...props />
     const PageLayout = props => (
       <div>
         <NavigationBar ...props />
         <Feed user={props.user} />
       </div>
     )
     const NavigationBar = props => (
       <Link href={props.user.permlink}>
         <Avatar user={props.user} size={props.avatarSize} />
       </Link>
     )

     const el = <Page user={user} avatarSize={20} />
     #+end_example
  2) HOC方式——传递底层组件和属性一同向下传递
     #+begin_example js
     // 中间组件PageLayout不用接收user和avatarSize属性
     const PageLayout = props => (
       <div>
         <NavigationBar userLink={props.userLink} />
         {props.content}
       </div>
     )
     const Page = props => {
       const userLink = (
         <Link href={props.user.permlink}>
           <Avatar user={props.user} size={props.avatarSize} />
         </Link>
       )
       const content = <Feed user={props.user} />

       return <PageLayout userLink={userLink} content={contetn} />
     }

     const el = <Page user={user} avatarSize={20} />
     #+end_example
* Error Boundaries
  React 16引入该特性，用于解决问题组件不会导致整个系统不可用的问题。
  Error Boundaries的处理流程：
  1) 捕获当前元素及其子元素的渲染函数、生命周期函数和构造函数中的异常
  2) 记录错误日志信息
  3) 渲染一个降级UI

  注意：Error Boundaries不获取以下异常
  1) Event Handlers
  2) Asynchronous code
  3) Server side rendering
  4) Errors thrown in the error boundary itself(rather than its children)

** 自定义Error Boundary Component
   #+begin_example js
   import React from "react"

   class ErrorBoundary extends React.Component {
     constructor(props){
       super(props)
       this.state = {hasError: false}
     }

     static getDerivedStateFromError(error){
       // 只要实现这个函数或componentDidCatch函数，那么该组件即为Error Boundary Component。
       // 该函数用于在捕获到异常时返回降级UI用的state，然后在render函数中根据state值显示渲染降级UI。
       return {hasError: true}
     }

     componentDidCatch(error, info){
       // 只要实现这个函数或static getDerivedStateFromError函数，那么该组件即为Error Boundary Component。
       // 该函数用于捕获异常时记录异常日志信息
       logger.error(error, info)
     }

     render(){
       return this.state.hasError ? (<h1>Something went wrong.</h1>) : this.props.children
     }
   }

   <ErrorBoundary>
     <FancyButton/>
   </ErrorBoundary>
   #+end_example

** 未捕获异常的默认行为
   As of React 16, errors that were not caught by any error boundary will result in unmounting of the whole React component tree.

** 异常消息堆栈
   异常消息堆栈包含如下信息：
   1) 调用链中的组件名称
      这里通过 ~Function.name~ 或 ~Function.displayName~ 获取组件名称，因此对于IE11以前的浏览器则需要使用 function.name-polyfill(https://github.com/JamesMGreene/Function.name) 处理。
   2) 组件所在文件名

** 事件和异步代码引发的异常
   由于Error Boundaries用于捕获导致组件渲染异常的问题，而事件和异步代码的异常在组件渲染阶段并不响应组件进行渲染，因此需要采用JS的 ~try/catch~ 处理。

* Lifecycle

** React v16前

*** 创建时
    | 生命周期函数       |
    |--------------------|
    | constructor        |
    | componentWillMount |
    | render             |
    | componentDidMount  |

*** 更新时
    | 生命周期函数              | 生命周期含素          |
    |---------------------------+-----------------------|
    | new props                 | states                |
    |---------------------------+-----------------------|
    | componentWillReceiveProps | shouldComponentUpdate |
    | shouldComponentUpdate     | componentWillUpdate   |
    | componentWillUpdate       | render                |
    | render                    | componentDidUpdate    |
    | componentDidUpdate        |                       |

*** 卸载时
    | 生命周期函数 |
    |--------------|
    | componentWillUnmount |

*** 生命周期函数详解
    1) componentWillMount
       在这里调用 ~this.setState~ 不会引起组件重新渲染，一般将这些内容移至 ~constructor~ 中执行。实际开发中很少用到。
    2) componentWillReceiveProps
       根据新props和旧props判断是否需要调用 ~this.setState~ 更新状态触发渲染。
       - 方法签名
         #+begin_example js
         componentWillReceiveProps(nextProps)
         #+end_example
    3) componentWillUpdate
       用于执行在更新发生前的工作。实际开发中很少用到。
       - 方法签名
         #+begin_example js
         componentWillUpdate(nextProps, nextState)
         #+end_example

** React v16.3
getDerivedStateFromProps仅发生两个地方
1) 创建时 constructor -> getDerivedStateFromProps
2) 更新时 new props ->  getDerivedStateFromProps

** React v16.4+
由于React 16推出Fiber，而之前的生命周期方法并不是和Fiber的async rendering，因此更新了生命周期方法。

*** 创建时
   | 阶段                                | 生命周期函数              |
   |-------------------------------------+---------------------------|
   | "Render阶段"                        | constructor               |
   | 纯净且没有副作用，                  | getDerivedStateFromProps  |
   | 可能会被React                       | UNSAFE_componentWillMount |
   | 暂停、中止或重新启动                | render                    |
   |-------------------------------------+---------------------------|
   | "Commit阶段"                        | React更新DOM和refs        |
   | 可以使用DOM，运行副作用，安排更新。 | componentDidMount         |

所有UNSAFE方法将在React 17被移除。

**** 父子组件生命周期函数执行顺序(排除unsafe方法)
1) 父constructor
2) 父getDerivedStateFromProps
3) 父render
4) 子constructor
5) 子getDerivedStateFromProps
6) 子render
7) 子componentDidMount
8) 父componentDidMount

*** 更新时
| 阶段                                                 | 生命周期函数1                                     | 生命周期函数2                   |
|------------------------------------------------------+---------------------------------------------------+---------------------------------|
|                                                      | (New Props)   setState()                          | forceUpdate()                   |
|------------------------------------------------------+---------------------------------------------------+---------------------------------|
|                                                      | UNSAFE_componentWillReceiveProps(仅New Props触发) |                                 |
| "Render阶段"                                         | static getDerivedStateFromProps                   | static getDerivedStateFromProps |
| 纯净且没有副作用，                                   | shouldComponentUpdate                             |                                 |
| 可能会被React                                        | UNSAFE_componentWillUpdate                        | UNSAFE_componentWillUpdate      |
| 暂停、中止或重新启动                                 | render                                            | render                          |
|------------------------------------------------------+---------------------------------------------------+---------------------------------|
| "Pre-commit阶段"还没实际地更新DOM状态，但可以读取DOM | getSnapshotBeforeUpdate                           | getSnapshotBeforeUpdate         |
|------------------------------------------------------+---------------------------------------------------+---------------------------------|
| "Commit阶段"                                         | React更新DOM和refs                                | React更新DOM和refs              |
| 已经更新了DOM，可以使用DOM，运行副作用，安排更新。   | componentDidUpdate                                | componentDidUpdate              |

所有UNSAFE方法将在React 17被移除。

**** 父子组件生命周期函数执行顺序(排除unsafe方法)
1) 父getDerivedStateFromProps
2) 父shouldComponentUpdate
3) 父render
4) 子getDerivedStateFromProps
5) 子shouldComponentUpdate
6) 子render
7) 子componentDidUpdate
8) 父componentDidUpdate

*** 卸载时
   | 阶段                                                            | 生命周期函数         |
   |-----------------------------------------------------------------+----------------------|
   | "Commit阶段" 已经更新了DOM，可以使用DOM，运行副作用，安排更新。 | componentWillUnmount |

*** 异常处理
    ~componentDidCatch(error, info)~
    1) 只能捕获子组件的异常，无法捕获自身的异常。
    2) 只有在生产环境才能生效，以下方式修改
       1) 通过 ~npm run build~ 打包发布
       2) 将项目模式从development调整为production
       3) 若使用create-react-app脚手架，则需要先解包 ~npm run eject~ ，然后修改 ~/scripts/build.js~ 的 ~process.env.NODE_ENV~ 为 ~production~ 后，运行 ~npm start~
    3) 实例
       #+begin_example js
       import React from "react"

       class MyErrorBoundary extends React.Component{
         state = {
           isError: false
         }

         render(){
           if (this.state.isError){
             return <div>Error</div>
           }
           else{
             return this.props.children
           }
         }

         componentDidCatch(error, info){
           console.log(error, info)
           this.setState({isError: true})
         }
       }
       #+end_example

**** 父子组件生命周期函数执行顺序(排除unsafe方法)
1) 子componentWillUnmount
2) 父componentWillUnmount

*** setState的异步函数写法
    不管是同步设置还是异步函数写法，连续多个setState的调用将被React合并为一个，并只触发一次渲染。
    1) 过去同步setState方式
       #+begin_example js
       this.setState({node: 1})
       #+end_example
    2) 异步函数写法
       #+begin_example js
       /*
        * @param {function(prevState, props)} - 通过异步函数写法确保prevState是上一次渲染后的state，若采用同步写法this.state获取的可能是本轮其它setState的值。
        * @param {function} callback - 在setState触发渲染完成后执行callback
        */
       this.setState((prevState, props) => {
         return props
       }, callback)
       #+end_example
*** forceUpdate
    父组件中调用forceUpdate会导致子组件的声明周期函数被触发。实际开发中很少用到。

*** 生命周期函数详解
   1) constructor
      1) 用于初始化内部状态和绑定方法
         #+begin_example js
         constructor(props){
           this.state = {type: 0}
           this.handleClick = this.handleClick.bind(this)
         }
         #+end_example
      2) 唯一可以直接修改state的地方
   2) getDerivedStateFromProps
      React 16.3 引入的新的生命周期函数，用于根据外部属性生成内部状态并作为函数返回值返回，然后会将返回值自动合并到组件的状态上。
      1) 为类静态方法
      2) 用于替代之前的componentWillReceiveProps
      3) 方法签名
         #+begin_example js
         /*
          * @param {object} nextProps - 新传入来的props
          * @param {object} prevState - 之前的state，在方法体内 this.state === prevState 返回true
          * @return {object} - 新的state，若不产生新的state则返回null
          */
         static getDerivedStateFromProps(nextProps, prevState){
           const {type} = nextProps
           if (type != prevState.type){
             return {type,}
           }
           return null
         }
         #+end_example
      4) 如果父组件的state进行了更新，那么其子组件也会触发它们的getDerivedStateFromProps方法。
      5) 使用该方式时必须通过constructor初始化state，否则会在控制台显示警告信息。
      6) 使用条件
         1) 无条件地更新状态
         2) 当props和state不匹配时更新state
      7) 尽量不使用：维护两者状态一致性会增加复杂度
         1) 直接在render中使用通过计算props的值更容易保持一致性，而不是另外设置到state上。
      8) 每次render都会调用到
      9) 典型场景：表单控件获取默认值
   3) render
      组件唯一必须定义的生命周期函数，用于定义DOM结构的地方。
      1) 方法签名
         #+begin_example js
         /*
          * @return {ReactElement|null}
          */
         render(){
         }
         #+end_example
   4) componentDidMount
      这个方法在刷新页面前触发的，因此在其中调用 ~this.setState~ 触发2次render，用户看到的效果也是页面被刷新一次。但会引起性能问题。
      - 用处：在这里发起异步请求等IO操作
   5) shouldComponentUpdate
      用于自定义某些操作是否需要更新UI，以便作性能优化。返回true为更新UI，false为不更新UI且中止当前流程。
      1) 在页面render之前调用，state已更新，DOM未更新但可访问
      2) 方法签名
         #+begin_example js
         /*
          * @param {object} nextProps - 最新的props
          * @param {object} nextState - 最新的state
          * @return {boolean} - True继续渲染，False中止当前组件渲染流程，但不影响子组件的更新渲染流程
          */
         shouldComponentUpdate(nextProps, nextState){}
         #+end_example
      3) 不能在方法体内调用 ~this.setState~
      4) 典型场景：获取render之前的DOM状态
   6) getSnapshotBeforeUpdate
      React 16.3 引入的新的生命周期函数
      1) 可以读取未更新时的DOM信息，但无法操作DOM，然后将信息传递给componentDidUpdate。
      2) 方法签名
         #+begin_example js
         /*
          * @param {object} prevProps - 变化前的props
          * @param {object} prevState - 变化前的state
          * @return {any}
          */
         getSnapshotBeforeUpdate(prevProps, prevState)
         #+end_example
      3) 示例-获取更新前的滚动条位置
         #+begin_example js
         class ScrollingList extends React.Component {
           constructor(props){
             super(props)
             this.listRef = React.createRef()
           }

           getSnapshotBeforeUpdate(prevProps, prevState){
             if (prevProps.list.length < this.props.list.length){
               const list = this.listRef.current
               return list.scrollHeight - list.scrollTop
             }

             return null
           }

           render(){
             return (
               <div ref={this.listRef}>
                 {this.list.map(item => <div key={item}>{item}</div>)}
               </div>
             )
           }

           componentDidUpdate(prevProps, prevState, snapshot){
             if (snapshot !== null) {
               const list = this.listRef.current
               list.scrollTop = list.scrollHeight - snapshot
             }
           }
         }
         #+end_example
   7) componentDidUpdate
      #+begin_example js
      /* @param {object} prevProps - 之前的props
       * @param {object} prevState - 之前的state
       * @param {any} dataFromGetSnapshortBeforeUpdate - getSnapshotBeforeUpdate函数的返回值
       */
      componentDidUpdate(prevProps, prevState, dataFromGetSnapshortBeforeUpdate)
      #+end_example
      1) 典型场景：页面需要更新props变化重新获取数据
   8) componentWillUnmount
      用于释放资源。
      1) 注意：在初始化阶段若当前组件和子组件的 ~componentDidMount~ 发生异常，也会触发 ~componentWillUnmount~ 的执行。
      2) 方法签名
         #+begin_example js
         componentWillUnmount(){}
         #+end_example

* TODO Integrating with other libraries
** Integrating with DOM Manipulation Plugins
   由于React并不知道DOM变化，而是自身维护一份Virtual DOM并映射到DOM中。若外界直接修改DOM，那么React再次通过Virtual DOM映射DOM时将蒙圈。
   因此当在React中使用其他DOM操作库时
   1) DOM操作的元素和React管理的元素要分离
   2) React初始化元素原始状态后，将控制权交给DOM操作，并在生命周期函数 ~componentWillUnmount~ 中执行DOM操作库的事件清理和其他善后工作
      #+begin_example js
      class MyComponent extends React.Component{
        componentDidMount(){
          this.$el = $(this.el)
          this.$el.component()
          this.$el.on("click", function(evt){})
        }

        componentWillUnmount(){
          this.$el.component("destroy")
          this.$el.off("click")
        }

        render(){
          return <div ref={el => this.el = el}></div>
        }
      }
      #+end_example

*** TODO 自定义Select

* Typechecking with PropTypes
  定义props接受的数据类型、是否必选和默认值
  #+begin_example js
  import PropTypes from "prop-types"

  class A extends React.Component{
    render(){
      return <div></div>
    }
  }
  // const A = props => <div></div> 同样适用

  // 定义props的数据类型
  A.propTypes = {
    name: PropTypes.string.isRequired,
    age: PropTypes.number,
    actoin: PropTypes.func,
    children: PropTypes.element, // 只能传递一个子元素
  }

  // 定义props的默认值
  A.defaultProps = {
    name: "john"
  }
  #+end_example
  警告信息将在浏览器的devtool中显示。

* Portals
  提供一种方式在当前元素之外渲染子元素。适合Dialog, Tooltips等组件开发。
  #+begin_example js
  /*
   * @param {ReactElement|DOMString|ReactFragment} child - 待渲染的子元素
   * @param {DOMElement} container - 渲染用的容器
   */
  ReactDOM.createPortal(child, container)
  #+end_example

** Event Bubbling Through Portals
   虽然DOM结构上通过Portal渲染的子元素不在当前元素其下，但在React内部维护的Virtual DOM Tree中，该元素依然为当前元素的子元素，因此如Context和Event冒泡等依然和常规的一致。
   #+begin_example js
   import React from "react"
   import ReactDOM from "react-dom"

   const modalRoot = document.getElementById("modal-rootp")
   class Modal extends React.Component {
     constructor(props){
       super(props)
       this.el = document.createElement("div")
     }

     componentDidMount(){
       // 这里是先将子元素渲染到detached DOM element上，然后在加入到render tree中，为的是方便后续移除
       // 若需要用到autoFocus属性，或在这里获取渲染后的尺寸，那么必须在render函数中将子元素直接渲染到位于render tree中的DOM element。
       modalRoot.appendChild(this.el)
     }

     componentWillUnmount(){
       modalRoot.removeChild(this.el)
     }

     render(){
       return ReactDOM.createPortal(this.props.children
                                    , this.el)
     }
   }

   const Parent = props => {
     const handleClick = evt => console.log(evt)

     return (
       <div onClick={handleClick}>
         <Modal>{this.props.children}</Modal>
       </div>
     )
   }

   const App = props => (
     <Parent>
       <button type="button" value="click"/>
     </Parent>
   )


   ReactDOM.render(<App/>, document.getElementById("root"))
   #+end_example;
* Refs and the DOM
  ref用于在render函数中引用DOM Element和React Element。
  常用使用场景：
  1) Managing focus, text selection, or media playback;
  2) Triggering imperative animations;
  3) Integerating with third-party DOM libraries.

  一般场景下使用声明式比指令式更好，如使用isOpen属性控制组件行为，而不是使用open和close函数。

** 使用方式1 ~React.createRef~
   React 16.3 引入的新特性。
   1) 创建(Function Component无法创建Ref)
      #+begin_example js
      class MyComponent extends React.Component {
        constructor(props){
          super(props)
          this.myRef = React.createRef()
        }

        render(){
          return <div ref={this.myRef}></div>
        }
      }
      #+end_example
   2) 访问
      #+begin_example js
      /* 1. 若ref指向HTML Element，则current指向的为DOM Element
       * 2. 若ref指向Class Component，则current指向Class Component实例
       */
      const node = this.myRef.current
      #+end_example
   3) ref指向HTML Element
      1) 在 ~componentDidMount~ 和 ~componentDidUpdate~ 前设置ref的值为HTML Element
      2) 在 ~componentWillUnmount~ 前设置ref的值为null
      3) 实例
         #+begin_example js
         class TextInput extends React.Component {
           constructor(props){
             super(props)
             this.textInput = React.createRef()
             this.focusTextInput = this.focusTextInput.bind(this)
           }

           focusTextInput(evt){
             this.textInput.current.focus()
           }

           render(){
             return (
               <div>
                 <input type="text" ref={this.textInput} />
                 <input type="button" value="focus" onClick={this.focusTextInput} />
               </div>
             )
           }
         }
         #+end_example
   4) ref指向Class Component
      #+begin_example js
      class Parent extends React.Component {
        constructor(props){
          super(props)
          this.textInput = React.createRef()
        }

        componentDidMount(){
          this.textInput.current.focusTextInput()
        }

        render(){
          return <TextInput ref={this.textInput} />
        }
      }
      #+end_example
   5) ref指向Function Component是无效的，因为Function Component是不会生成实例的
      #+begin_example js
      const TextInput = props => <input type="text" />
      class Parent extends React.Component {
        constructor(props){
          super(props)
          this.textInput = React.createRef()
        }

        render(){
          // 无效的
          return <TextInput ref={this.textInput} />
        }
      }
      #+end_example

** 使用方式2 Callback Refs
   1) 创建
      1) 在 ~componentDidMount~ 和 ~componentDidUpdate~ 前设置ref的值为HTML Element
      2) 在 ~componentWillUnmount~ 前设置ref的值为null
      3) 实例
         #+begin_example js
         class TextInput extends React.Component {
           constructor(props){
             super(props)
             this.textInput = null
             this.focusTextInput = this.focusTextInput.bind(this)
             this.getTextInputRef = ref => {this.textInput = ref}
           }

           focusTextInput(evt){
             if (this.textInput){
               this.textInput.current.focus()
             }
           }

           render(){
             return (
               <div>
                 // 注意若使用inline function的形式 <input type="text" ref={ref => {this.textInput = ref}} />
                 // 那么这个inline function会被调用两次，导致this.textInput会先初始化为null，在设置为元素。
                 // 原因是每次调用render函数时都会生成新的inline function实例，而React会传递null来清空原有ref再设置新值。
                 <input type="text" ref={this.getTextInputRef} />
                 <input type="button" value="focus" onClick={this.focusTextInput} />
               </div>
             )
           }
         }
         #+end_example
   2) 实现forwarding ref效果
      #+begin_example js
      const Child = props => <input type="text" ref={props.inputRef} />
      const Parent = props => {
        let refs = {}
        const getInputRef = ref => {
          refs.inputRef = ref
        }

        return <Child inputRef={getInputRef} />
      }
      #+end_example
* Render Props
作为一种设计模式——函数作为子组件，通过prop传递function来在多个组件间共享代码。从而实现由组件使用者控制如何动态显示组件的内容；若以元素作为子组件则无法灵活控制动态显示，因为调用时已经决定只能显示传入的子元素。
React-Router, Downshift 和 React-Motion 都使用该技术实习。
#+begin_example js
class MyComponent extends React.Component {
  render(){
    return (
      <div>
        {this.props.children("Hello!")}
      </div>
    )
  }
}

const el = (
  <MyComponent>
    {name => <div>{name}</div>}
  </MyComponent>
)
#+end_example

** 示例——分享鼠标当前位置
#+begin_example js
class Mouse extends React.Component {
  constructor(props){
    super(props)
    this.state = {x: 0, y: 0}
    this.handleMouseMove = this.handleMouseMove.bind(this)
  }

  handleMouseMove(evt){
    this.setState({x: evt.clientX, y: evt.clientY})
  }

  render(){
    return (
      <div onMouseMove={this.handleMouseMove}>
        {this.props.children(this.state)}
      </div>
    )
  }
}

const Cat = props => <div>x: {props.x}; y: {props.y}</div>

const mouseTracker = Component => (
  <Mouse>
    {({x, y}) => <Component x={x} y={y} />}
  </Mouse>
)

// 直接使用Render Props
const CatTracker = props => (
  <Mouse>
    {({x, y}) => <Cat x={x} y={y} />}
  </Mouse>
)
// 使用HOC+Render Props
const CatTracker2 = mouseTracker(Cat)
#+end_example

** 提供Render Props的组件即成React.PureComponent的注意点
问题代码：
#+begin_example js
class Mouse extends React.PureComponet{
  constructor(props){
    super(props)
    this.state = {x: 0, y: 0}
    this.handleMouseMove = this.handleMouseMove.bind(this)
  }

  handleMouseMove(evt){
    this.setState({x: evt.clientX, y: evt.clientY})
  }

  render(){
    return (
      <div>
        {this.props.children(this.state)}
      </div>
    )
  }
}

const CatTracker = props => (
  <Mouse>
    {({x, y}) => <div>x: {x}, y: {y}</div>}
  </Mouse>
)
#+end_example
由于CatTracker向Mouse的render props赋予inline fucntion，因此每次调用CatTracker的render时都会创建新的inline function实例，导致PureComponent无效。
因此要修改为向Mouse的render props赋予一个方法实例
#+begin_example js
function renderCat({x, y}){
  return <div>x: {x}, y: {y}</div>
}
const CatTracker = props => <Mouse>{renderCat}</Mouse>
#+end_example
* React.PureComponent
  为React 15.3 引入的新特性，用于取代之前的PureRenderMixin，减少不必要的render操作。
  适合用于一些纯展示状态的组件上。

** 原理
   通过shallowEqual排除不必要的render
   1) 通过 ~Object.keys(state)~ 和 ~Object.keys(props)~ 比较prev和next的长度是否一致；
   2) prev和next是否拥有相同的key(值不进行比较)；
   3) prev和next指向的地址是否一致。

   也就是仅对比第一层，当上述三个条件之一不符合则触发渲染，否则中止当前组件的渲染流程(子组件继续)。

** 与shouldComponentUpdate共存时
   规则
   1) 存在shouldComponentUpdate时则以shouldComponentUpdate结果为准；
   2) 没有shouldComponentUpdate时则按PureComponent的shallowEqual结果为准。

** 实例
   当父组件调用render函数时就会触发子组件调用render函数，当实现 PureComponent 后若传递给子组件的props内存地址不变，则不会触发子组件的render函数。

* TODO HOOKS
* TODO Suspends
* Strict Mode
  作为工具用于发现潜在问题，和Fragment一样不会影响UI状态，而是提供对其子元素额外的检查和警告。
  并且仅影响开发环境，并不会影响生产环境。

  使用方式
  #+begin_example js
  function App(){
    return (
      <div>
        <Header/>
        <React.StrictMode>
          <div>
            <ComponentOne />
            <ComponentTwo />
          </div>
        </React.StrictMode>
        <Footer/>
      </div>
    )
  }
  #+end_example

  作用：
  1) Identify components with unsafe lifecycle
     由于部分历史生命周期函数(如 ~componentWillMount~)在异步应用中会引发问题，通过 ~<React.StrictMode>~ 包裹第三方组件可发现其是否仍然使用存在问题的生命周期函数。
  2) Warning about legacy string ref API usage
     可以使用 callback ref 或 createRef 替代 string ref。
  3) Warning about deprecated findDOMNode usage
     可以使用 forwarding ref 替代 findDOMNode。
  4) Detecting unexpected side effects
     React的工作过程可分为Render阶段和Commit阶段(先忽视Pre-commit阶段)，Render阶段用于确定变更范围，Commit阶段用于执行变更。其中Commit阶段执行速度很快，但Render阶段有可能执行很慢，导致阻塞UI线程。因此React将推出异步Render阶段，即会多次调用Render阶段的生命周期函数，因此需要Render阶段的生命周期函数是可多次执行且幂等的。
     - Render阶段的生命周期函数
       - constructor
       - componentWillMount
       - componentWillReceiveProps
       - componentWillUpdate
       - getDerivedStateFromProps
       - shouldComponentUpdate
       - render
       - setState with updater function as the first argument
     - Strict Mode会调用两次以下生命周期函数，从而确定它们的幂等性
       - constructor
       - render
       - setState with updater function as the first argument
       - getDerivedStateFromProps
  5) Detecting legacy context API

* 引入样式

** CSS
   1) 使用
      #+begin_example js
      // index.js
      import React from "react"
      import ReactDOM from "react-dom"
      import './reset.css' // 引入样式，Webpack会处理
      import App from "./App"

      ReactDOM.render(<App />,
                      document.getElementById("root"))
      #+end_example
   2) Webpack配置

** CSS in JS

*** Styled-components
    1) 定义全局样式
       #+begin_example js
       import {injectGlobal} from "styled-components"

       injectGlobal`
         body {
           margin: 0;
           padding: 0;
           font-family: sends-serif;
           background: green;
         }
       `
       #+end_example
    2) 定义局部样式
       #+begin_example js
       import styled from "styled-components"
       import logoPic from "../../statics/logo.png" // 引入图片，Webpack自动处理

       // 定义一个带样式的div组件，别名为HeaderWrapper
       // 相当于定义 const HeaderWrapper = props => <div ...props style={{height: "56px", background: "red"}}>props.children</div>
       export const HeaderWrapper = styled.div`
         height: 56px;
         background: red;
       `

       export const Log = styled.a.attrs({
         href: "/"
       })`
         background: url(${logoPic});
       `

       // 使用时通过className启用样式
       // <NewItem /> 不启用.left和.right样式，仅应用height样式
       // <NewItem className="left" /> 启用.left样式和height样式
       export const NewItem = styled.div`
         height: 55px;

         &.left {
           float: left;
         }

         &.right {
           float: left;
         }
       `
       #+end_example

* TODO 后端通信
** axios
API说明：https://www.kancloud.cn/yunye/axios/234845

1) 安装 ~npm install axios --save~
2) 引入 ~import axios from "axios"~
** mock data
1) 在./public/api/下建假数据文件，请求/api/...即可访问。
2) Mockjs
   - 可拦截Ajax根据请求参数，响应json或plain text
   - 无法修改response http status

** 通信请求和响应数据结构定义
json-schema
JavaScript实现：ajv

* TODO i18n
  1) yahoo的react-i18n
     仅适用于JSX的国际化，无法JSX和JS共享一套国际化机制。
  2) alibaba的react-i18n-univeral(https://github.com/alibaba/react-intl-universal)
     JSX和JS共享一套国际化机制。
  3) react-i18next

** TODO react-intl-universal
* Redux
redux-logger在开发环境使用的中间件，需要通过环境变量来控制在哪个环境打包进去。
状态管理框架，基于flex架构。
整个应用的所有组件的状态都保存在一个store中。

优点：
1) 确定性状态视图(与纯组件结合时启用确定性视图呈现)
2) 事务状态
3) 状态管理与IO和副作用隔离
4) 应用状态使用单一信息源
5) 在不同组件间轻松共享状态
6) 事务遥测(自动记录操作对象)
7) 跟踪调试

特性：
1) Single Source of Truth
2) 可预测性
   state+action=new state
3) 纯函数更新store

** 核心概念
1) Store
   #+begin_example js
   /* 创建Store实例
    * @param {function} reducer - 纯函数
    * @return {Store}
    */
   const store = createStore(reducer)

   /* 从Store实例中获取当前状态
    */
   store.getState()
   /* 将一个action(如点击某按钮)分发给某个reducer
    */
   store.dispatch(action)
   /* 订阅Store实例中状态的变化，变化发生时回调listener方法。
    * 回调方法中通过store.getState()获取Store中的数据，然后决定是否要调用组件的this.setState触发更新
    * @return {function} unsubscribe - 退订，在componentWillUnmout中调用来释放资源
    */
   store.subscribe(listener)

   store.replaceReducer
   #+end_example
2) Action
   用于描述行为
   #+begin_example js
   {
     type: ADD_TODO,
     text: "Build my first Redux app"
   }
   #+end_example
3) Reducer
   用于接收action并根据action更新state
   #+begin_example js
   function todoApp(state=initialState, action){
     switch(action.type){
       case ADD_TODO:
         return Object.assign({}, state, {
           todos: [
             ...state.todos,
             {
               text: action.text,
               completed: false
             }
           ]
         })
       default:
         return state
     }
   }
   #+end_example
   - 注意：
     1) 通过dispatch分发的action，该store中的所有reducer都能接收到，因此需要reducer自行选择执行。
** 数据更新流程
| View | Action         | Store      |
|------+----------------+------------|
|      | - dispatch ->  | Dispatcher |
|      |                | $       $  |
|      |                | Reducer    |
|      |                | $       $  |
|      | <- subscribe - | State      |
** 工具函数
1) combineReducers
   将多个reducer合并为单个reducer
   #+begin_example js
   import {combindReducers} from "redux"
   function todos(state = [], action){
     switch(action.type){
       case "ADD_TODO":
         return state.concat([action.text])
       default:
         return state
     }
   }

   function counter(state = 0, action){
     switch(action.type){
       case "INCREMENT":
         return state + 1
       case "DECREMENT":
         return state - 1
       default:
         return state
     }
   }

   const reducer = combineReducers({
     todos,
     counter
   })
   #+end_example
2) bindActionCreators
   - 构建action函数和分发action时会分为以下两步
     #+begin_example js
     /* Action Creater
      * @param {any} payload - action承载的内容
      * @return {action}
      */
     function addTodo(payload){
       return {type: "ADD_TODO",
               payload: payload}
     }

     store = createStore(reducer)

     // 1. 产生action
     // 2. 分发action
     store.dispatch(addTodo("Hello!"))
     #+end_example
   - 通过bindActionCreators可将上述两步合并
     #+begin_example js
     /* Action Creater
      * @param {any} payload - action承载的内容
      * @return {action}
      */
     function addTodo(payload){
       return {type: "ADD_TODO",
               payload: payload}
     }

     store = createStore(reducer)

     // 生成一个包含产生action和分发action的函数
     {addTodo} = bindActionCreators({addTodo: addTodo}, store.dispatch.bind(store))

     // 使用时一步完成产生action和分发action的函数
     addTodo("Hello!")
     #+end_example
** 集成React
   需要通过中间件react-redux集成。

*** 工作原理
    | 原组件的props | 高阶组件 | --> | Actions  |
    |               | (通过    |     | $     $  |
    |               | connect  |     | reducers |
    |               | 封装     |     | $     $  |
    | 原组件的props | 原组件)  | <-- | Store    |

*** 示例-计数器
    #+begin_example js
    import {createStore, bindActionCreators} from "redux"
    import {Provider, connect} from "react-redux"

    // redux
    const counter = (state = {count: 0}, action) => {
      switch(action.type){
        case "INCREMENT":
          return {count: state.count + 1}
        case "DECREMENT":
          return {count: state.count - 1}
        default:
          return state
      }
    }

    store = createStore(counter)

    let increment = () => {type: "INCREMENT"}
    let decrement = () => {type: "DECREMENT"}

    // react component
    class Counter extends React.Component {
      render(){
        // 调用connect后默认会将dispatch作为props传递给组件
        const {count, increment, decrement, dispatch} = this.props
        return (
          <div>
            <button onClick={decrement}>-</button>
            <span>{count}</span>
            <button onClick={increment}>+</button>
          </div>
        )
      }
    }

    // 通过connect连接
    /* 自动订阅store的状态变化，一旦变化则调用该方法，从Store中提取state作为传给component的props
     * 若connect函数中不传递该函数，则不订阅和不响应store的状态变化。
     * @param state - store.getState()的返回值
     * @param {any} [ownProps] - 组件自带的属性，若函数中声明这个参数，那么当组件自身的props发生变化也会触发该方法
     * @return {object}
     */
    const mapStateToProps = (state, ownProps) => {count: state.count}
    /* 生成包含action creator和dispatch两步的函数作为传给component的props
     */
    const mapDispatchToProps = (dispatch, ownProps) => bindActionCreators({increment, decrement}, dispatch)
    // 生成高阶组件
    const ConnectedCounter = connect(mapStateToProps, mapDispatchToProps)(Counter)

    // 使用
    const App = props => (
      <Provider store={store}> // 作为整个系统的根节点
        <ConnectedCounter />
      </Provider>
    )
    #+end_example
** 异步Action
   1) 工作原理
      在Action和Dispatcher之间添加一个Middleware层，用于拦截异步Action和转发异步Action的内和最终产生的Action给Dispatcher。
   2) 创建Store实例时需提供中间件
      #+begin_example js
      import {createStore, applyMiddleware} from "redux"

      /* applyMiddleware
       * @param {object|array} middleware
       */
      const store = createStore(
        reducer,
        applyMiddleware([middleware]))
      #+end_example
   3) 中间件 redux-thunk
      1) 使用示例
         #+begin_example js
         import {createStore, applyMiddleware} from "redux"
         import thunk from "redux-thunk"

         const store = createStore(
           reducer,
           applyMiddleware(thunk))

         // 定义异步Action Creator
         function getList(){
           // 异步Action
           // 入参dispatch为Middleware传入，用于转发异步Action内产生的Action给Dispatcher
           // 必须返回Promise对象
           return dispatch => {
             dispatch({type: "BEGIN"})

             const promise = new Promise((resolve, reject) => {
               const doRequest = axios.get("....")
               doRequest.then(res => {
                 dispatch({type: "SUCCESS", data: res.data})
                 resolve(res)
               }, err => {
                 dispatch({type: "FAIL", data: {error: err}})
                 reject(err)
               })
             })
             return promise
           }
         }

         function reducer(state = {}, action){
           swith(action.type):
             case "BEGIN":
               return {
                 ...state,
                 fetchListPending: true,
                 fetchListError: false
               }
             case "SUCCESS":
               return {
                 ...state,
                 data: action.data
                 fetchListPending: false,
                 fetchListError: false
               }
             case "FAIL":
               return {
                 ...state,
                 fetchListPending: false,
                 fetchListError: true
               }
             default:
               return state
         }

         class List extends React.Component {
           constructor(props){
             super(props)
             this.state = {data: []}
             this.handleSearch = this.handleSearch.bind(this)
           }

           handleSearch(e){
             const action = getList()
             store.dispatch(action)
           }

           render(){
             return (
               {this.state.data.map(item => <div key={item}>{item}</div>)}
             )
           }

           componentDidMount(){
             this.unsubcribe = store.subscribe(()=>{
               let state = store.getState()
               this.setState({data: state.data})
             })
           }

           componentWillUnmount(){
             this.unsubcribe(
           }
         }
         #+end_example
   4) 中间件 redux-saga
      将异步action统一放置到sagas.js中管理，异步action定义为generator函数。
      在超大型项目中书写方式优于redux-thunk。
      #+begin_example js
      // store/index.js
      import {createStore} from "redux"
      import createSagaMiddleware from "redux-saga"
      import todoSagas from "./sagas"

      const sagaMiddleware = createSagaMiddleware()
      const store = createStore(reducer,
                                applyMiddleware(sagaMiddleware))
      sagaMiddleware.run(todoSagas)

      export default store
      #+end_example
      #+begin_example js
      // store/sagas.js
      import {takeEvery, put} from "redux-saga/effects"

      // 异步actionCreator
      function* getInitList(){
        try{
          const res = yield axios.get("/list.json")
          const action = initListAction(res.data)
          yield put(action)
        }catch(e){
          yield put({type: ERROR})
        }
      }

      // 定义一个除reducer外可以接收action的generator function
      function* todoSagas(){
        yield takeEvery(GET_INIT_LIST, getInitList)
      }

      export default todoSagas
      #+end_example
      #+begin_example js
      class TodoList extends React.Component{
        componentDidMount(){
          const initListAction = initList()
          store.dispatch(initListAction)
          this.unsubscribe = store.subscribe(() => {
            const state = store.getState()
            this.setState(state.data)
          })
        }

        componentWillUnmount(){
          this.unsubscribe()
        }
      }
      #+end_example

** Action和Reducer的组织方式
   原则：
   1) 一个Action和其对应的Reducer放在同一个文件，文件名为Action名；
   2) 另外提供一个包含所有Action引用的文件，和一个包含所有Reducer引用的文件。


   - 目录结构：
     #+begin_example
     project/
       - actions/
           - add-todo.js
           - delete-todo.js
       - actions.js
       - reducers.js
       - constants.js
     #+end_example
   - actions/add-todo.js
     #+begin_example js
     import {ADD_TODO} from "../constants.js"

     export const addTodo = (payload) => {type: ADD_TODO, payload}
     export const addTodoReducer = (state, action) => {
       switch(action.type){
         case ADD_TODO:
           return {
             ...state,
             todo: state.todo.concat(action.payload)
           }
         default:
           return state
       }
     }
     #+end_example
   - actions/delete-todo.js
     #+begin_example js
     import {DELETE_TODO} from "../constants.js"

     export const deleteTodo = (payload) => {type: DELETET_TODO, payload}
     export const deleteTodoReducer = (state, action) => {
       switch(action.type){
         case DELETET_TODO:
           return {
             ...state,
             todo: state.todo.filter(item => item != action.payload)
           }
         default:
           return state
       }
     }
     #+end_example
   - actions.js
     #+begin_example js
     export {addTodo} from "./actions/add-tod"
     export {deleteTodo} from "./actions/delete-tod"
     #+end_example
   - reducers.js
     #+begin_example js
     import combineReducers from "redux"
     export {addTodoReducer} from "./actions/add-todo"
     export {deleteTodoReducer} from "./actions/delete-todo"

     export default combineReducers({addTodoReducer, deleteTodoReducer})
     #+end_example
* TODO 不可变数据
   1) 原生写法
      #+begin_example js
      const state = {filter: "completed", todos: ["Learn React"]}

      const newState = {
        ...state,
        todos: [
          ...state.todos,
          "Learn Redux"
        ]
      }

      const newState2 = Object.assign({}, state, { todos: [
        ...state.todos,
        "Learn Redux"
      ]})
      #+end_example
   2) Immutable.js
      1) 安装
         #+begin_example bash
         npm install immutable --save
         #+end_example
      2) 使用
         #+begin_example js
         import {fromJS} from "immutable"

         // 将JS对象转换为immutable对象
         const defaultState = fromJS({
           focused: false
         })

         let focused = defaultState.get("focused")

         let newState = defaultState.set("focused", true)
         #+end_example
      3) 集成Redux —— redux-immutable
         让整个state变为immutable对象
         #+begin_example js
         import { combineReducers } from "redux-immutable"

         const reducer = combineReducers({
           header
         })

         export default reducer
         #+end_example
         #+begin_example js
         const mapStateToProps = state => {
           return {
             // 等价于 focused: state.get("header").get("focused")
             focused: state.getIn(["header", "focused"])
           }
         }
         #+end_example
* 异步组件
  1) 安装模块
     #+begin_example bash
     npm install @babel/preset-react @babel/plugin-syntax-dynamic-import --save-dev
     npm install react-loadable --save
     #+end_example
  2) 使用
     #+begin_example js
     import Loadable from "react-loadable"

     // 定义异步组件
     const LoadableComponent = Loadable({
       loader: () => import("./MyComponent"), // 指定将哪个普通组件封装为异步组件
       loading(){
         // 定义加载时的状态
         // 若使用jsx语法，那么文件中必须写import React from "react"
         return <div>Loading</div>
       }
     })

     export default class App extends Component{
       render(){
         return <LoadableComponent/>
       }
     }
     #+end_example
     1) 异步组件和react-router集成的问题
        react-router会将其数据以props的方式传递给异步组件，但内部的普通组件却无法得到从react-router传来的值(如this.props.match.params)。因此需要通过如下方式再封装
        #+begin_example js
        import {withRouter} from "react-router"
        class MyComponent extends Component{

          componentDidMount(){
            const {match, location, history} = this.props
            const getDetailAction = getDetail(match.params.id)
            store.dispatch(getDetailAction)
          }
        }

        export default withRouter(MyComponent)
        #+end_example

* React Router
** 安装
   #+begin_example bash
   npm install react-router-dom --save
   #+end_example

** 基本使用和三种路由方式
   #+begin_example js
   import React from "react"
   import {render} from "react-dom"
   import {
     BrowserRouter as Router, // HTML5的URL路由
     // HashRouter as Router,    Hash路由
     Route,
     Link
   } from "react-router-dom"
   // import {MemoryRouter as Router} from "react-router" 内存路由，一般SSR中使用


   const Home = props => <h1>Home</h1>
   const Hello = props => <h1>Hello</h1>
   const About = props => <h1>About Us</h1>

   export default App = props => (
     <Router>
       <ul>
         {props.routes.map(({path, text})=>(
           <li key={path}>
             <Link to={path}>{text}</Link>
           </li>
         ))}
       </ul>
       <div>
         {props.routes.map(({path, component})=>(
           <Route path={path} component={component} />
         ))}
       </div>
     </Router>
   )

   const routes = [
     {path: "/home", text: "Home", component: Home},
     {path: "/hello", text: "Hello", component: Hello},
     {path: "/about", text: "About", component: About}
   ]
   render(App(routes), document.getElementById("root"))
   #+end_example
** 核心元素
   1) ~<Link>~ 普通链接，不会触发浏览器刷新
      #+begin_example js
      /*
       * @param {string|object|function} to
                object时格式为 {pathname, search, hash, state}，其中state: { fromDashboard: true }??
       * @param {boolean} replace
       * @param {(node)->null|RefObject} innerRef - 指向元素引用
       */
      <Link to="/home">Home</Link>
      #+end_example
   2) ~<NavLink>~ 类似Link但会添加当前选中状态
      #+begin_example js
      /*
       * @param {string} activeClassName
       * @param {object} activeStyle
       * @param {boolean} exact
       * @parma {boolean} strict
       * @param {(match, location) -> boolean} isActive - 添加逻辑判断是否启用active状态
       */
      <NavLink to="/home" selectedClassName="selected">Home</NavLink>
      #+end_example
   3) ~<Prompt>~ 路由切换时，若满足条件则提示用户是否离开当前页面
      #+begin_example js
      import {Prompt} from "react-router"

      <Prompt
        when={formIsHalfFilledOut}
        message={location => `Are you sure you want to leave ${location.pathname}?` }
      />
      #+end_example
   4) ~<Redirect>~ 重定向当前页面，如登录判断
      #+begin_example js
      import {Route, Redirect} from "react-router"

      <Route exact path="/" render={props=>(
        loggedIn ? (
          <Redirect to="/dashboard" />
          // <Redirect to={{
          //             pathname: "/dashboard",
          //             state: {from: props.location}
          //           }}/>
        ) : (
          <LoginPage />
        )
      )} />
      #+end_example
   5) ~<Route>~ 路由配置的核心标记，路径匹配时显示对应组件
      #+begin_example js
      import {Route} from "react-router-dom"

      /* 1. 通过属性component定义匹配时显示的子组件 */

      // 默认采用贪婪匹配，而添加exact则表示采用精准匹配
      // 若为<Route path="/" component={Home}/> 则所有路由都会让Home组件生效。
      // 并且路由匹配并不是排他的，只要匹配多个路由都会生效。
      // 可通过:name的方式定义从路由获取参数，作为props传递给组件。
      <Route exact path="/" component={Home}/>
      <Route path="/news" component={News}/>
      <Route path="/user/:id" component={User}/>

      // 若属性path值为空则必定命中该路由，而其match object则为父Route的match object，若无则为null。
      <Route component={NotFound}/>

      /* 2. 通过属性render定义匹配时显示的子组件
       * 若和属性componnet同时出现，则采用component的设置。
       */
      <Route render={
        (props) => (<div>props.match.url</div>)
      }/>

      /* 3. 通过属性children定义显示的子组件，无论是匹配与否
       * 当匹配时match不为null，当不匹配时match为null。
       * 然后通过match是否为空来控制显示的子组件。
       * 在三种方式中，这种方式优先级最低。
       */
      <Route path="/hi">
        {({match}) => (
          !match && (
            <div>
              没有命中时显示的内容，并且此时的match object为null。
            </div>
          )
        )}
      </Route>

      #+end_example
      - 传递到子组件的route props
        - ~match~
        - ~location~
        - ~history~
      - 属性
        - ~{string|string[]} path~
        - ~{bool} exact=false~
        - ~{bool} strict=false~
          - 当path值以 ~/~ 结尾，如 ~/one/~ 那么 ~/one~ 将匹配失败，而 ~/one/~ 或 ~/one/two~ 则匹配成功；
          - 配合 ~exact=true~ 当path值为 ~/one~ 那么仅能匹配 ~/one~
        - ~{bool} sensitive=false~ 是否大小写敏感
        - ~{object} location~
          1) 默认为 ~window.location~ ，但可以传递另一个location给Route，那么Route将用这个locaiton的pathname来和path作匹配。
          2) 若外层Switch组件也定义了属性location，那么将使用Switch提供的。
   6) ~<Switch>~ 只显示第一个匹配的路由
      #+begin_example js
      import {Switch, Route} from “react-router"

      <Switch>
        <Route exact path="/" component={Home} />
        <Route path="/news" component={News} />
        <Route path="/user/:id" component={User} />
        <Route component={NoMatch} />
      </Switch>
      #+end_example

** 核心API
   1) match object
      包含属性path值匹配URL的Route的相关信息，若Route组件的属性path值为空则表示必定命中，match object的内容为最近的一个父Route的match object。(若父的为null，那么子也为null)
      1) ~{object} params~ 包含从URL中提取的动态参数，如 ~/topic/:id~
      2) ~{string} url~ URL匹配部分(不是整个URL)，一般用于作为嵌套Link的属性to值前缀，如 ~<Link to={`${this.props.match.url}/rendering`}>Rendering</Link>~
      3) ~{string} path~ Route元素的属性path值，一般用于作为嵌套Route的属性path值前缀，如 ~<Route path={`${this.props.match.path}/:topicId`}>~
      4) ~{boolean} isExact~ 是否全匹配
   2) history object
      - 包含的属性
        1) ~{number} length~ 历史栈长度
        2) ~{string} action~ 当前动作(PUSH,REPLACE,POP)
        3) ~{object} location~ 当前location对象
           1) ~{string} path~
           2) ~{string} search~
           3) ~{string} hash~
           4) ~{object} state~ 动作相关参数，仅在BrowserRouter和MemoryRouter中可用。
           5) ~push(path: string|location, [state])~ 将新的地址压栈
           6) ~replace(path: string|location, [state])~ 用新的地址替换栈顶地址
           7) ~go(n)~ 跳转到从当前位置向前(n为负数)或向后(n为正数)的栈帧地址
           8) ~goBack()~ 等价于 ~go(-1)~
           9) ~goForward()~ 等价于 ~go(1)~
           10) ~block(prompt)~ 在地址发生变化前向用户提示进行二次确认
               #+begin_example js
               const unblock = history.block("Are you sure?")
               const unblock2 = history.block((location, action) => {
                 if (input.value !== "") return "Are you sure?" // 进行二次确认
                 else return null // 不进行二次确认
               })

               unblock() // 解除二次确认
               #+end_example
      - 注意：history object是可修改的，因此不要依赖它的location来作props变更前后的对比。
        #+begin_example js
        componentDidUpdate(prevProps){
          // 永远都是true
          console.log(prevProps.history.location == this.props.history.location)
        }
        #+end_example
   3) location object
      不可变对象，因此作props变更前后对比时应该采用location object。
   4) withRouter
      用于向高阶组件传递最新的location、history和match对象。
      1) 基本使用
         #+begin_example js
         // ./show-location.js
         import React from "react"
         import { withRouter } from "react-router"

         class ShowLocation extends React.Component {
           render(){
             const {location, match, history} = this.props
             return <div>{location.pathname}</div>
           }
         }

         export default withRouter(ShowLocation)
         #+end_example
      2) 通过Component.WrappedComponent引用底层组件
         #+begin_example js
         import React from "react"
         import ShowLocationWithRouter from "./show-location"

         export default() => <ShowLocationWithRouter.WrappedComponent location={{pathname:"test"}}/>
         #+end_example
      3) 通过wrappedComponentRef获取底层组件
         #+begin_example js
         import React from "react"
         import ShowLocationWithRouter from "./show-location"

         let node = null
         const callbackRef = ref => {node = ref}

         export default() => <ShowLocationWithRouter wrappedComponentRef={callbackRef}/>
         #+end_example

** 通过URL传递参数
   路由参数匹配库：https://github.com/pillarjs/path-to-regexp

   #+begin_example js
   import {Route, Link} from "react-router"

   const Topic = props => (
     <div>
       {props.match.params.id}
     </div>
   )

   <Link to="/topic/2">Topic/2</Link>
   <Route path="/topic/:id" component={Topic} />
   #+end_example
** 嵌套路由
   每个React组件都可以为路由容器。
   #+begin_example js
   import {Route, Link} from "react-router"
   import {BrowserRouter as Router} from "react-router-dom"

   const Page = pros => (
     <div>{`id: ${props.match.params.id}; subid: ${props.match.params.subid}`}</div>
   )

   const SubCategory = props => (
     <div>
       {
         (props.match.params.id == 1)
         ? <Link to="/category/1/sub/1">S11</Link>
         : <Link to="/category/2/sub/1">S21</Link>
       }

       <Route path="/category/:id/sub/:subid" component={Page}/>
     </div>
   )

   const App = props => (
     <div>
       <Link to="/category/1">C1</Link>
       <Link to="/category/2">C1</Link>
       <Route path="/category/:id" component={SubCategory}/>
     </div>
   )
   #+end_example
** 管理路由授权
   要求：
   1) 区分受保护路由和公开路由
   2) 访问未授权路由时重定向到登录页面

** Scroll Restoration

单页面跳转时浏览器会保留滚动条的位置，可以通过自定义元素处理。
通用方案的基本准则：
1) Scrolling up on navigation so you don't start a new screen scolled to the bottom.
2) Restoring scroll positions of the window and overflow elements on "back" and "forward" clicks.

定义：
#+begin_example js
import React from "react"
import {withRouter} from "react-router"

const schema = {
  type: "array",
  items: {
    type: "object",
    properties: {
      pathname: {
        type: "string"
      },
      x: {
        type: "number"
      },
      y: {
        type: "number"
      }
    }
  }
}

class ScrollRestoration extends React.Component {
  render(){
    return null
  }

  componentDidUpdate(prevProps){
    const {location} = prevProps
    if (this.props.location.pathname !== location.pathname){
      this.positions = [{pathname: location.pathname, x: window.scrollX, y: window.scrollY}]
                       .concat(this.positions.filter({pathname} => pathname !== location.pathname))

      const currPosition = this.positions.find(({pathname}) => pathname === this.props.location.pathname)
      if (currPosition) {
        window.scrollTo(currPosition.x, currPosition.y)
      }
      else {
        window.scrollTo(0, 0)
      }
    }
  }

  componentDidMount(){
    this.positions = JSON.parse(sessionStorage.getItem("scroll-restoration") || "[]")
    const {location} = this.props
    const currPosition = this.positions.find(({pathname}) => pathname === location.pathname)
    if (currPosition) {
      window.scrollTo(currPosition.x, currPosition.y)
    }
  }

  componentWillUnmount(){
    sessionStorage.setItem("scroll-restoration", JSON.stringify(this.positions))
  }
}

export default withRouter(ScrollRestoration)
#+end_example
使用
#+begin_example js
import ScrollRestoration from "./scroll-restoration"

export default() => (
  <Router>
    <ScrollRestoration>
      <Switch>
        <Route path="/" exact component={Main}/>
        <Route path="/contact" component={Contact}/>
      </Switch>
    </ScrollRestoration>
  </Router>
)
#+end_example

* TODO UI组件库
** Ant.Design
   面向企业级应用的UI库。

** Material UI
   基于Google Material UI设计风格的UI库，面向终端用户。
* TODO Next.js创建同构应用
  约定：
  1) 页面是pages目录下的一个组件
  2) static目录放置静态文件
  3) page具有特殊静态方法 ~getInitialProps~
  4) components目录放置标准的React组件
  5) 每个page会单独打包

** 示例
   #+begin_example yaml
   components/
   node_modules/
   pages/
    - user/
    - about.js
    - index.js
   static/
    - nextjs.jpg
   package.json
   #+end_example

   index.js
   #+begin_example js
   // 和react-router的Link一样为前端路由不刷页面
   import Link from "next-link"

   export default () => (
     <div>
       <img src="/static/next.js.jpg" />
       <nav>
         <Link href="/about"><a>About</a></Link>
       </nav>
     </div>
   )
   #+end_example
** 核心API
   1) Link标记
      #+begin_example js
      import Link from "next/link"

      /* 前端路由不会引发浏览器刷新页面
       * @param {DOMString} href - URL
       * @param {boolean} prefetch - 默认情况下是切换时加载页面资源，但可以通过prefetch属性让页面空闲时就加载资源
       * @param {boolean} replace - 浏览器的History中用最新的URL替换原有URL
       */
      <Link href="/about">About</Link>
      #+end_example
   2) 动态加载
      #+begin_example js
      import dynamic from "next/dynamic"

      // 动态加载../components/hello2的组件
      const DynamicComponentWithCustomLoading = dynamic(
        import("../components/hello2"),
        {
          loading: () => <p>...</p>
        }
      )

      export default () => (
        <div>
          <Header />
          <DynamicComponentWithCustomLoading />
        </div>
      )
      #+end_example
* TODO 单元测试
  React让前端单元测试变得简单：
  1) React应用很少需要访问浏览器API
  2) 虚拟DOM可以在NodeJS环境中运行和测试
  3) Redux隔离了状态管理，很好地分离了UI层和数据层，因此可以进行纯数据层单元测试

** 测试工具
   1) Jest: Facebook开源的JS单元测试框架
   2) JS DOM: 浏览器环境的NodeJS模拟
      #+begin_example js
      const JSDOM = require("jsdom").JSDOM

      global.window = new JSDOM("<!DOCTYPE html><div id="react-root"></div>").window
      global.document = window.document
      global.navigator = window.navigator
      global.HTMLElement = window.HTMLElement
      #+end_example
   3) Enzyme: React组件渲染和测试
   4) nock: 服务端模拟HTTP请求
      #+begin_example js
      // 后续所有发向这个URL的请求都会返回500
      nock("http://xxx")
        .get("/r/react.js")
        .reply(500, null)
      #+end_example
   5) sinon: 函数模式和调用跟踪
   6) istanbul: 单元测试覆盖率
* TODO 开发调试工具
  1) ESLint 语法检查和代码一致性
     - 使用.eslintrc进行规则配置
     - 一般使用airbnb的代码风格规则
       #+begin_example bash
       npm install eslint-config-airbnb --dev-save
       #+end_example
  2) Prettier 用于格式化代码
     - 使用.prettierrc进行规则配置
  3) React DevTool

  4) Redux DevTool
* 架构
** 理想架构
   - 易于开发
     1) 开发工具完善度
     2) 生态圈是否繁荣
     3) 社区是否活跃
   - 易于维护
     1) 代码是否容易理解，一致的
     2) 文档是否健全
   - 易于构建
     1) 使用通用技术和架构
     2) 构建工具的选择
   - 易于测试
   - 易于扩展
     1) 新增功能是否容易
     2) 新功能是否会显著增加系统复杂度

** 按领域模型(feature)拆分复杂度
按领域模型(feature)组织代码，降低耦合度
将 action, reducer 和 routing 按 feature 分类归类组织。
目录结构
#+begin_example yaml
yarn.lock
package.json
.gitignore
README.md
node_modules/
public/
src/
  - App.js
  - App.css
  - index.js
  - index.css
  - serviceWorker.js
  - statics/
  - features/
      - common/  # 共用组件
          - redux/ # action,reducer统一放在这里
              - reducers.js
              - actions.js
              - add-todo.js
              - del-todo.js
          - component1.js # 组件，组件内不直接加载样式，避免样式的重复加载和打包
          - component1.less # 组件对应的样式
          - index.js # 加载所有组件的入口
          - style.less # 加载所有样式的入口
          - constants.js # 常量
      - home/ # 领域，其下结构和common一样
#+end_example
* TODO 拖放的实现
  强大的第三方库：react-beautiful-dnd
* TODO 性能优化
** 常见的性能问题场景
   1) 键盘输入
   2) 鼠标移动

** 按需加载
   使用Webpack的import API，让Webpack从这里单独打包。
   使用react-loadable库实现React异步加载，提供加载期间的loading状态的展现，和加载失败的降级显示。

** 使用Reselect避免重复计算
   其实就是Memorize
* TODO Micro Task 和 Macro Task
** Macro Task
   setTimeout, setInterval, requestAnimationFrame 发起的异步任务将被放置 Macro Task Queue。
   Macro Task Queue 可以存在多个。
   Macro Task Queue 的第一个任务就是入口JavaScript程序。

** Micro Task
   Promise, process.nextTick 发起的异步任务将被放置 Micro Task Queue
   Micro Task Queue 仅能存在一个。
   执行规律为：每个事件循环先执行完 Micro Task Queue 中所有任务，然后再执行 Macro Task Queue 的一个任务。
* TODO 工程化
1) Skeleton
2) jira
   1) task arrangement(hour, staff, hours per week)
   2) progress of task(todo, in process, testing, done)
3) GitLab
   1) GitHub flow
   2) Prettier
      https://www.infoq.cn/article/using-prettier-format-javascript-code
   3) Code review
4) Jenkins
   1) Unit Test
   2) Conver Test
   3) Sonar
   4) Bundle
5) Nexus
6) Ansible
7) Docker+k8s

* TODO 源码剖析
* TODO PWA
* QA
1) 若使用JSX那么文件必须包含 ~import React from "react"~ ，否则无法识别JSX。
